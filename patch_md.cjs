const fs = require('fs');
let md = fs.readFileSync('PROJECT_STATE.md', 'utf-8');

md = md + `
- **Реализация механики седловой сцепки (Fifth-Wheel Coupling)**:
  - Решена проблема "раскачивания" (вобблинга) полуприцепа (trailer_semi) влево-вправо на скорости. Для \`trailer_semi\` в физике (\`physics.ts\`) включена полноценная механика "второй бочки" (Tractrix curve) с персистентным задним мостом, что обеспечивает абсолютно стабильное следование за тягачом без обратной связи и колебаний.
  - Устранена терминология "дышла" для полуприцепов. Взаимодействие (клавиша **E**) теперь корректно отображает термины "Шкворень" и "Седло" (например, "Расшплинтовать седло", "Защелкнуть шкворень в седле").
  - Добавлена **автоматическая сцепка шкворня**. Если игрок сдает на тягаче \`truck_semi\` задним ходом (скорость < 0) и попадает седлом (hitchOffset) прямо под шкворень полуприцепа (couplerOffset), замок защелкивается автоматически. Игроку остается только выйти из кабины и подключить тормозные шланги и кабель освещения.
`;
fs.writeFileSync('PROJECT_STATE.md', md);
